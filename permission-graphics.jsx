// permission-graphics.jsx — stub (permission step removed from onboarding flow)
function PermissionsScreen({ onDone }) {
  useEffect(() => { if (onDone) onDone(); }, []);
  return null;
}
Object.assign(window, { PermissionsScreen });
