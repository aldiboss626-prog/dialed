# dependencies
node_modules/

# secrets — NEVER commit these. Hand-carry them to a new device (see HANDOFF.md).
.env
.env.*
**/.env
**/.env.*

# build output
client/dist/
dist/

# server SQLite runtime data (regenerated + seeded on startup)
server/data/

# secret-bearing demo seeder (contains the Supabase SERVICE-ROLE key) — hand-carry, do not push
lose stuff/

# Claude Code local machine settings (not portable)
.claude/settings.local.json
.claude/._settings.local.json
.claude/.codex/

# OS / editor junk
.DS_Store
Thumbs.db
*.log
