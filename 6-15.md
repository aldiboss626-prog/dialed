# Dialed — Session Log "6/15" (worked 2026-06-16)

Branch: **`v2-redesign`** · everything below is `tsc --noEmit` clean.
Read this first next session, then continue.

---

## 🧭 Direction we're heading
1. **v2 redesign** → a focused **two-pane "Network · Inbox"** app (swipeable, no bottom tab bar). Full roadmap in the plan file `~/.claude/plans/okay-give-me-a-purrfect-waffle.md`.
2. **Notifications are the core value** — the app proactively says "hit up Sam about Google." Keep the opportunity/nudge engine; cut the Opportunities/Tracker/Learn *pages* (archive, don't delete).
3. **Launch track** (separate): Gmail API (paused til Fri), payments (RevenueCat + EAS), Railway deploy.

---

## ✅ Shipped this session
**Theme**
- Reverted the warm cream → **clean white palette** in `theme.ts` (`CalColors`: white bg, near-black ink, blue accent). Added `CategoryColors` (Mentor/Friend/Recruiter/Professor).

**New screens / features**
- **Reply/compose** (`app/reply.tsx`) rebuilt to match the screenshot: paste email *or* upload screenshot → AI draft, inline "Reply preview", Generate / Copy / Open in Mail / Mark as replied.
- **Settings** (`app/settings.tsx`) redesigned: centered title + back, icon tile on every row, chevrons, category-colored cadence rows, PRO/FREE badge, **Sign Out + Delete Account**.
- **Profile / analytics page** (`app/profile.tsx`, NEW): reached by tapping the **avatar in the Home header**; **⚙ gear → Settings**. Shows overall network health as a **dial**, stat tiles, a **podium** (top-3 strongest connections) + list for the rest, and a "what your score means" explainer.
- **Home vs Profile now measure different things**: Home hero = **"This week"** momentum (count of who needs you), Profile = **overall 0–100 health**.
- **Dials** (the app's signature, thermostat/stove-knob tick style): `components/DialMotion.tsx` (animated count dial on Home — red=down, green=up, full=hold) and `components/DialScore.tsx` (fills to a 0–100 score on Profile).
- **Manual contact notes/history**: add timestamped entries on the contact detail (`contactsDb.addNote` + composer); timeline now shows all entries newest-first (also fixed an `i.notes`→`i.note` bug).
- Shared primitives built for the future shell: `components/Segmented.tsx`, `components/Reveal.tsx`.

**AI quality + safety (server)**
- Reply drafts: text path **Haiku → Sonnet 4.6** (`server/src/routes/content.js`).
- **Personalization loop**: drafts learn the user's voice from their own sent replies (`sent_replies` table + `sentRepliesDb` + `styleExamples`).
- **Cost protection on `/api/content/*`** (`server/src/middleware/contentGuard.js`): Supabase-JWT auth + per-user **40/day** rate limit + 8k input cap. Verified (401 no-token, 200 with demo token). Mobile attaches the token via `contentHeaders()` in `contentApi.ts` (all 9 content calls + 6 learn pages).
- **Account deletion** endpoint `DELETE /api/account` (`server/src/routes/account.js`) + the Delete Account button.

---

## ⚠️ Pending manual steps (do these to fully activate)
1. **Supabase SQL editor** — run both (saved at repo root):
   - `migrate_sent_replies.sql` → activates personalized drafts.
   - `migrate_interaction_notes.sql` → activates manual contact notes (adds `'note'` type).
2. **`SUPABASE_SERVICE_ROLE_KEY`** → add to `server/.env` (+ Railway) so account deletion works (currently returns "not configured" — safe).
3. **Rotate secrets before launch**: the Google client secret (shown in chat) + the Supabase service-role key (precaution).

---

## ⏸️ Gmail / Google auth — PAUSED, resume **Fri Jun 19** (payday → Apple Dev $99)
Full status + steps in **`GMAIL_AUTH_PROGRESS.md`** (deep detail in `GMAIL_API_VERIFICATION_PLAN.md`).
- Done: Google Cloud project "Dialed", People API on (confirm Gmail API), consent screen Testing + test users, 5 scopes, **Web OAuth client created** (Client ID + Secret saved outside repo).
- Next: Apple Developer account → **EAS iOS dev build** → iOS OAuth client → wire the Connect Gmail flow (Claude does the wiring).

---

## 🗺️ v2 migration — what's LEFT (from the plan file)
- **Phase 3** — NetworkPane (health hero + needs-attention + opportunity nudge cards).
- **Phase 4b** — InboxPane list (email rows, priority sort, inbox-zero). *(Compose screen already done.)*
- **Phase 5** — Two-pane **SwipeDeck shell** (Segmented Network|Inbox, no tab bar); archive old tabs/learn.
- **Phase 6** — Wire cadence + opportunity nudges into push + notifications screen + Network cards.
- **Phase 7** — Finish re-skin: contacts list + contact detail. *(Settings already redesigned.)*
- **Phase 8** — Onboarding redesign (last).
- **Section A** — Supabase RLS/data gap migration (some addressed by the two migrations above).

---

## 🛠️ How to run / test
- Mobile: new terminal → `cd mobile && npx expo start` (LAN — **`--tunnel` is broken on this Windows ARM64 machine**). Scan QR.
- Server (needed for AI features): `cd server && npm run dev` (port 3001).
- **Demo login**: `demo@dialed.app` / `dialeddemo123` (Pro, seeded data). AI now **requires being logged in** + the server running.

## 🆕 New skill installed
- **`emil-design-eng`** (Emil Kowalski's UI-craft/animation design-engineering skill) at `.claude/skills/` — **active next session** (skills load at startup). Use it to polish dials, transitions, the v2 panes.

## ▶️ Next session pickup
Likely: **Phase 3 NetworkPane** (the core of the v2 redesign), or keep iterating on features. Gmail resumes Friday.
